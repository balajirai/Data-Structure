#include <iostream>
#include <vector>
using namespace std;
 
int main(){
    vector<int> myvector{ 1, 2, 3, 4, 5 };
    cout << myvector.size();    //Returns the number of elements in the vector.
    return 0;
}