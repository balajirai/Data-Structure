// This function is used to find the length of the string in terms of bytes.
// length()

#include <iostream>
using namespace std;
int main(){
    string s1 = "Balaji Rai";

    // length in terms of bytes
    int len = s1.length();

    cout << "length of the string is : " << len;
    return 0;
}