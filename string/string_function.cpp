#include <bits/stdc++.h>
using namespace std;
int main(){

    // suppose we want a string having k characters of 'A'
    int k = 5;

    // string(frequency, char)
    string s = string(k, 'B');

    cout << s << endl;

    return 0;
}