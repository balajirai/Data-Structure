#include <iostream>
using namespace std;
int main(){

    int n,m;
    cout<<"Enter two numbers to swap  : ";
    cin>>n>>m;

    swap(n,m);
    cout<<"After swapping numbers are : "<<n<<" "<<m<<endl;
return 0;
}